# /end — Session End (Smart Routing)

> The global `/end` command auto-detects `.agents/` and runs project close-out + knowledge capture.
> No project-level override needed — the global command handles both modes.

See `~/.claude/commands/end.md` for full protocol.
