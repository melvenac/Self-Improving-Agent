# /end — Session End (Smart Routing)

> This is a local copy. The canonical version lives at `~/.claude/commands/end.md`.
> It auto-detects `.agents/` and runs project close-out + knowledge capture, or lightweight capture only.

See the global command for full details. This file exists so the command is discoverable from this repo.
