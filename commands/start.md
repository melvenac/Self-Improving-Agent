# /start — Session Start (Smart Routing)

> This is a local copy. The canonical version lives at `~/.claude/commands/start.md`.
> It auto-detects `.agents/` and runs full project startup + knowledge recall, or lightweight recall only.

See the global command for full details. This file exists so the command is discoverable from this repo.
